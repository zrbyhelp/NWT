# AI 配置

AI 配置按当前登录账号隔离。每个账号可以维护自己的 OpenAI-compatible 供应商、LLM 模型、向量模型和图片模型。

## 供应商

供应商保存以下信息：

- 名称
- 标识
- Base URL
- API Key
- 启用状态

API Key 使用 `AI_CONFIG_ENCRYPTION_KEY` 加密后写入数据库。编辑供应商时不会回显明文，只能重填或清空。

## LLM 模型

LLM 模型用于首页聊天回复。字段包括：

- 供应商
- 显示名称
- 模型 ID
- 默认温度
- 启用状态
- 是否默认

同一账号同一时间只保留一个启用且供应商可用的默认 LLM。首页聊天必须使用当前登录账号自己的默认 LLM；如果当前账号没有可用默认 LLM，系统不会用 `.env.local` 代替，而会提示用户前往右上角“体验设置”配置 LLM 模型。

若默认 LLM 所属供应商缺少 API Key，也会提示前往“AI 供应商”页补全配置。`.env.local` 中的 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL` 只作为没有用户上下文的内部兜底配置。

## 向量模型

向量模型用于后续文本切分、语义检索和 Qdrant 写入。字段包括：

- 供应商
- 显示名称
- 模型 ID
- 向量维度
- 最大输入 tokens
- 启用状态
- 是否默认

当前版本先完成配置、启停和默认项管理。

## 图片模型

图片模型用于后续封面、场景图、角色图和场景全景生成。场景全景会在前端表单里选择画面类型，再传给服务端提示词。字段包括：

- 供应商
- 显示名称
- 模型 ID
- 启用状态
- 是否默认

当前版本先完成配置、启停和默认项管理；具体图片生成工作流已接入封面、假面设定板和场景全景的生成链路。

## 模型列表

新增或编辑模型时，前端会请求服务端动作，由服务端使用当前账号保存的 API Key 请求：

```text
{baseUrl}/models
```

返回列表会做基础分类：

- LLM：常见聊天模型，如 `gpt`、`qwen`、`deepseek`、`claude`。
- 向量模型：常见 embedding 模型，如 `embedding`、`bge`、`e5`。
- 图片模型：常见图片模型，如 `gpt-image`、`dall-e`、`flux`、`stable-diffusion`。

如果供应商不支持 `/models` 或列表不完整，仍可手动输入模型 ID。

拉取模型列表也属于外部 AI 供应商访问，服务端会写入 Langfuse span，记录供应商、Base URL、状态和模型数量。非测试环境必须先配置主应用的 `LANGFUSE_PUBLIC_KEY` 与 `LANGFUSE_SECRET_KEY`。

## 默认项规则

默认模型选择遵循：

- 只有模型启用且供应商启用时，才可作为默认项。
- 用户主动设为默认时，会取消同类型其他默认项。
- 如果没有可用默认项，系统会选取最早创建的可用模型。
- 如果没有可用模型，所有默认标记会被清空。
